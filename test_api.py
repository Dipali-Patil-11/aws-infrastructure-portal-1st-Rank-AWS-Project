import requests
import json

# Start a session
s = requests.Session()

# The frontend passes credentials to login
# Since I don't have the real credentials, I can't login via the API...
# Wait, I don't have the AWS credentials!
